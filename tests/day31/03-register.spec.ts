import { test, expect } from "@playwright/test";

test("Register Test", async ({ page }) => {
  // Test Senaryosu: Yeni bir kullanıcının register formu üzerinden başarılı şekilde kayıt olması
  //
  // Test Adımları:
  // 1. https://ecommerce-playground.lambdatest.io/index.php?route=account/register sayfasına gidilir.
  // 2. Faker ile rastgele kullanıcı bilgileri oluşturulur.
  // 3. First Name alanı doldurulur.
  // 4. Last Name alanı doldurulur.
  // 5. E-Mail alanı doldurulur.
  // 6. Telephone alanı doldurulur.
  // 7. Password alanı doldurulur.
  // 8. Password Confirm alanı aynı şifre ile doldurulur.
  // 9. Privacy Policy kabul edilir.
  // 10. Continue butonuna tıklanır.
  // 11. Kayıt işleminin başarılı olduğu doğrulanır.
});