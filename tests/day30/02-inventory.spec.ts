import { test, expect } from "@playwright/test";



test("inventory item test", async ({ page }) => {
  // Test Senaryosu:
  // 1. Login sayfasına git

  
  // 2. "standard_user" kullanıcı adı ve "secret_sauce" şifresi ile giriş yap
  // 3. Ürün listesi sayfasının (inventory) yüklendiğini doğrula
  // 4. Sayfadaki tüm ürün elementlerini bir diziye al
  // 5. Dizinin uzunluğunun 6 olduğunu doğrula
});